$('#status-<%=@company.id%>').text("<%= @company.verified ? 'Approved' : 'Pending'%>")
$('#link-<%=@company.id%>').text("<%= !@company.verified ? 'Approve this company' : 'Disapprove this company'%>")

